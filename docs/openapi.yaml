openapi: 3.1.0
info:
  title: Sub2API
  version: 0.0.0
  description: API documentation for Sub2API. This file is intentionally incomplete and is a work in progress.
servers:
  - url: /
paths: {}
components:
  schemas:
    ResponseEnvelope:
      type: object
      description: Common API envelope.
      properties:
        code:
          type: integer
        message:
          type: string
        reason:
          type: string
        metadata:
          type: object
          additionalProperties:
            type: string
        data:
          nullable: true
      required:
        - code
        - message
    ErrorResponse:
      type: object
      description: Middleware error response.
      properties:
        code:
          type: string
        message:
          type: string
      required:
        - code
        - message
